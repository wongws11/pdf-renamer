requests>=2.31.0
Pillow>=10.0.0
pdf2image>=1.16.3

# Note: You also need to install poppler separately
# Mac: brew install poppler
# Ubuntu/Debian: sudo apt-get install poppler-utils
# Windows: Download from https://github.com/oschwartz10612/poppler-windows/releases/